# 剪辑工具 · 全量待办 Backlog（工具所有还没做的）
> 刷新: 2026-08-13 深夜 | 源: `OpenCut对照诊断报告.md` + `timeline-toolbar.tsx` + 实际代码状态
> **铁律**: 功能缺口不凭空补，等用户对标 OpenCut 指定某功能 → 读源码 → 翻译实现。一个工具一个工具来，看 OpenCut 数据流。
> **改 HTML 必须重启 `start.bat`**（PyWebView 启动加载一次，不热更新）。

## 〇、已完工（地基，别当缺口）
- 多轨(视频/音频/文本)、拖拽移动、跨轨移动、左/右双向裁剪、分割三态(S/Q/W)
- 撤销/重做(真实变更入栈) + 快捷键 Ctrl+Z/Y、[ ] 裁剪到播放头
- 同轨重叠自动避让、音频波形、画布自动匹配、缩放/seek
- 导入素材(点+拖放)、双击进轨(ffprobe 真实时长)、导入剪映项目、导出剪映草稿
- 状态共享 draft_state.json + MCP(get_state 0.5s 轮询；5 tool)
- 已根治: 删除涟漪(bug#1)、_push_undo 时序(bug#2)、split 浅拷贝(bug#3)、状态多引用幽灵 bug

## 一、编辑能力缺口（功能层，对照 OpenCut 诊断报告）
### P0 手感底线
- [x] **吸附对齐**：已对齐 OpenCut snapping 全局（build 段端点+播放头排除自身 / threshold 10px随zoom / resolve 最近点 / 蓝线 indicator / move+resize+播放头三处触发 + N键+snapBtn 开关）。🔵 已完工

### P1 垂直 skill 差异化
- [ ] **变速/重定时 retime**：口播/跳剪必备（加速铺垫/慢动作强调）。读 retime/ speed/
- [ ] **关键帧/动画 keyframes**：运镜推拉摇移/缩放/透明度。读 animation/ keyframes/ graph-editor
- [ ] **特效及参数 effects**：原待办点名项 → 升级为「基础特效原语引擎」(见四)
- [ ] **分组多选移动/缩放 group**：多段整体拖/缩放。读 group-move/ group-resize/

### P2 高级
- [ ] **遮罩 masks**：自定义点+反转。读 masks/
- [ ] **字幕 ASR 转录**：当前仅 SRT 导入，无识别。读 subtitles/ transcription/
- [ ] **贴纸/图形轨 stickers**：数据模型无此轨类型。读 stickers/ graphic/
- [ ] **书签 + 播放头吸附 bookmarks**：后端无 bookmark 数组。读 bookmarks/

### OpenCut 工具栏有、我们无（补进来的）
- [x] **波纹编辑 Ripple editing**：删/裁短右边界/同轨左移/split_left删右半 → 后续整体收拢(OpenCut diff 引擎移植，原子撤销)。🔵 已完工
- [x] **提取原声 toggle-source-audio**：选中视频段→ffmpeg 抽独立 m4a 音轨+原视频 volume=0 静音+音频轨生成配对段；再点还原。🔵 已完工（注：因 pyJianYingDraft AudioMaterial 拒视频文件，必须真抽 m4a，非 OpenCut 的逻辑引用）

## 二、工具栏图标 + 快捷键对齐（UI 层）
> 对照 OpenCut `timeline-toolbar.tsx` 真实按钮。✅已做 ⬜待做
### 左栏
- [x] ✂ Split (S/Ctrl+B) / ◀✂ Split-left (Q) / ✂▶ Split-right (W)
- [x] ⧉ Duplicate 复制（Ctrl+D + dupBtn 已接，后端 duplicate_segment 深拷同轨紧跟）
- [x] 🗑 Delete (Del)
- [x] 🔗 提取原声 → 见一·OpenCut补充
- [ ] ❄️ Freeze frame → OpenCut 自己 coming soon，**跳过**
- [ ] 🔖 书签 → 见一·P2
- [ ] 📈 GraphEditor 缓动曲线 → 属关键帧 P1，**明天不做**
### 右栏
- [x] 🧲 吸附 Auto snapping (N 键 + snapBtn，蓝线 indicator) → 见一·P0
- [x] 〰️ 波纹编辑(R 键 + rippleBtn，4 调用点传参 + split_left 后 seek) → 见一·OpenCut补充
- [x] 🔍 zoom ±/slider
### 轨道头部开关（OpenCut `TrackLabelsPanel`）
- [x] 🔊/🔇 轨道静音（audio/video 轨）：切换 `_track_meta[type][ti].muted` → **预览+导出**都生效：预览时 audio 轨跳过、video 轨内嵌音频 muted；导出时 audio 轨跳过、video 轨 `volume=0`。🔵 已完工
- [x] 👁/🚫 轨道隐藏（video/text 轨）：切换 `_track_meta[type][ti].hidden` → **预览+导出**都跳过该轨画面。🔵 已完工
- [x] 时间轴左侧轨道头部与右侧素材区垂直滚动对齐：底部水平滚动条只在右侧，左侧补等高的 `scrollbar-spacer` 并实时同步 `scrollTop`（参考 OpenCut `TrackLabelsPanel` 底部占位条）。🔵 已完工
- [ ] 🔒 轨道锁定：占位，暂未实现（防止误编辑）

### 快捷键-only（definitions.ts 有，我们没接）
- [x] 复制/粘贴 Ctrl+C / Ctrl+V（后端 copy_to_buffer + paste_from_buffer 内存剪贴板，按类型落首轨、保持相对间距）
- [x] 全选 Ctrl+A / 取消 Esc（selectedKeys 多选集合 + Ctrl/⌘+click toggle；Delete 整批删）
- [x] 导航 j/k/l(±1s)/Home·End(起止)/Shift+←→(跳5s)/←→(帧步进)：纯前端 seekPlayhead(clamp+重绘蓝线+时间码)，播放头不进草稿（对齐 OpenCut playback.seek），无需后端 set_playhead（之前误判，已纠正）
- [x] 播放 Space / K（Space 在；K 可补）
- [x] 撤销/重做 Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y

## 三、Skill 层（产品核心，当前空 slot）
> 架构定位：OpenCut 右上 PropertiesPanel 我们换成「Skill 区」——这个坑位现在空的，B 线要填的就是这格。
- [ ] **Skill 区 UI 接入**：工作台右侧面板，列可用 skill、触发、显示进度/日志
- [ ] **口播 skill**（第一 skill）：文字 + 响度情绪处理
- [ ] **跳切 skill**：细粒度抽草稿(禁全量)，按节奏跳切
- [ ] **垂直行业 skill**：财经口播(highlight+counter+zoom+chart) / 情感口播(shake+popText+zoom+flash) / 知识科普(typewriter+slide+diagram+highlight)
- [ ] **基础特效原语引擎（新战略，见四）**：Skill 调用的底层原语库

## 四、基础特效原语引擎（GPT 战略，下一阶段，非明天）
> 不抄剪映特效库（会做爆），先做 Agent 可调用原语，Skill 组合。
- **原语候选(10~20)**：fade/scale/move/rotate/shake/zoom/blur/mask/crop/slide/typewriter/popText/highlight/counter/progress/wipe/glitch/flash/bounce
- **架构闭环**：Skill → 调原语(MCP tool) → 改 segment 的 `effects` 字段 → 时间轴实时预览 → 导出剪映草稿
- **蒙版分层**：先 rectangle/circle/rounded-rect/linear-wipe/feather/invert；tracking/贝塞尔/人物分割后做
- **参考开源**（引用前先让 Codex 做 LICENSE 审查）：Remotion(特殊许可证商业闭源需商业许可) / Motion Canvas(程序化2D动画, 有 examples) / motion-editor(React+Vite+Remotion, 已含 Timeline+Keyframe+Text+Image+SVG+Video+Audio+Groups+MP4导出+AI助手)
- 把原「P1 特效(读 OpenCut effects/)」升级为自造原语引擎，优先级高于逐条抄

## 五、架构/后端层
- [ ] **导出层泛化**：export_draft 只建模 video/audio/text；加 sticker/effect/mask 轨需补 append_track 分支（bug#5）
- [ ] **多进程撤销互不覆盖**：MCP 独立进程写同文件，并发 undo 冲掉彼此改动（bug#4）
- [ ] **轻量读取接口**：Skill 读草稿禁全量(80万字符爆 token)；补 get_material_peaks/get_segment_peaks/get_track_text 等（跳切铁律）

## 六、备注
- 铁律：功能缺口不凭空补，等指定才做
- 工作方式：一个工具一个工具来，看 OpenCut 数据流
- 改 HTML 重启 start.bat
