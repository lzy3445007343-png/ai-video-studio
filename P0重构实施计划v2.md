# P0 重构实施计划 v2.2（架构基线·第一刀，按 GPT 三次校准修订）

> **纪律**：本计划经「用户 + GPT」双重校准后，再分步落地。落地前绝不动业务代码。
> 每步独立可回退 + 抽内联 JS 跑 `node --check` + **必须重启 `start.bat`** 真机验（PyWebView 不热更新）。
> 单文件约束：唯一改动文件 `工作台v0.8时间轴.html`。`main.py` 本轮尽量不动。
> 目标不是"修好某个 bug"，是让「改 clip 绝不触发媒体重建」成为架构事实。

**v2.2 变更（GPT 三次校准）**：① `ensureAllMediaReady` 全量预建 → `ensureCurrentPlaybackWindowReady` **窗口化预建**（避免 1000 素材点播放卡死）；② 新增"执行纪律"硬约束（本轮只许动 Step1，禁越界）；③ Step1 拆 **A/B/C 三轮**，每轮独立真机验；④ I2 补 phase2 media identity 说明；⑤ 补 Step2 排序说明（可前置但维持现状）。

---

## 〇、Phase 0 媒体调用地图（只读审计，已完成，基于 2026-08-13 当前代码）

> 方法：grep 全部 `createElement('video'/'audio'/'img')` / `.play()` / `.pause()` / `.load()` / `.currentTime=` / `src=` / `renderPreview` 调用链 / `isPlaying` 修改点。结论：**所有 media 元素的创建、src 设置、append、移除都集中在 `renderPreview()`（L1152）一个函数里**——它同时拥有媒体生命周期。播放中重建媒体的唯一通路是 `playTick` 跨段分支直接调 `renderPreview()`。

| 类别 | 位置 | 归属 | 播放中是否危险 |
|---|---|---|---|
| 创建 video | L1109 `createElement("video")` | renderPreview 内 `_setVisualContent` | 重建点 |
| 创建 img | L1117 / L1268 | renderPreview 内 | 视觉层，非音频 |
| 创建 audio | L1309 `createElement("audio")` | renderPreview 内 audio 段 | 重建点 |
| 设 src (video) | L1110 `v.src=fileURL(path)` | renderPreview | 重建点 |
| 设 src (image) | L1291 `rec.img.src=fileURL(seg.path)` | renderPreview | 视觉层 |
| 设 src (audio) | L1318 `rec.el.src=src` | renderPreview | 重建点 |
| play（主入口） | L1408 / L1416 / L1456 / L1463 | `playAllMedia()` | 受控，250ms 防抖 |
| **play（第二处）** | **L2794 `t.play()`** | **canplay 全局委托** | **⚠️ 竞态源，待删** |
| pause | L1339（失活音频）/ L1383 / L1386（playAllMedia） | renderPreview / playAllMedia | 安全 |
| currentTime= | L1138 `el.currentTime=t` | `_seekMedia()` | ✅ 唯一正确 seek |
| renderPreview 调用 | L1372 startPlay（播放前） | — | 合理 |
| **renderPreview 调用** | **L1544 playTick 跨段** | playTick | **🔴 根因 P-1：播放中重建媒体** |
| renderPreview 调用 | L1830 / L2826 / L2937 编辑态交互 | — | 合理（结构变更后） |
| renderPreview 调用 | L1846 `renderPreviewMaybe` | `if(isPlaying) return` 守卫 | 已挡 |
| isPlaying | L1050 定义 / L1365=true / L1377=false | — | ✅ **单一状态源确认** |

**地图结论**：
1. 媒体所有权完全集中在 `renderPreview`，且它既服务"编辑态结构变更"也服务"播放态跨段"——这是根。
2. 当前"播放中媒体被杀"的唯一通路 = **L1544 playTick 跨段调 `renderPreview()`**（跨段时触发整批媒体重定位/重挂 oncanplay，是播放卡顿/AbortError 的主要触发点）。L1304 之前的 `load()` 杀害已在前面轮次修成 `pause+muted`，所以现在的缺口集中在 L1544 这一处。
5. ⚠️ **2026-08-13 二次读码校正**：当前 `renderPreview()` 已演进为「按轨道复用元素」（`video:ti`/`audio:ti` 单元素跨段复用，src 相同则不重置，见 L1166-1342），**并非真的 destroy+create**。因此"裸删 L1544 renderPreview"会引入新 bug：跨段进入一个"播放起点未覆盖到的段"（如 t=0 起播、音频首段在 t=10）时，该段媒体元素从未被创建 → 静默不播。→ Step1 采用"播放前一次性预建所有媒体 + 跨段只 seek 不 render"的安全改法，而非裸删。
3. `canplay` 委托 L2794 是 `playAllMedia` 之外的**第二处 `play()`**，与跨段重建竞态 → 同一元素两次 play，后者 abort 前者。
4. `isPlaying` 单一源（GPT 担心的"多状态源"已用代码推翻）。

---

## 一、为什么 v1 要修订（GPT 校准要点，已吸收）

1. **Step1 跨度太大**：当前 3000+ 行单 HTML 里 UI/状态/播放/渲染/同步混在一起。直接让 AI "抽 Player" 会犯"建个对象复制播放代码"的错；真正要的是**改变媒体生命周期所有权**。→ 拆出 Phase 0 审计前置；Step1 不抽"漂亮 Player 对象"，只做到**播放期间媒体不死**。
2. **Player key 第一版用 `clip.id`**（不过早优化复用）；先保证"一个 clip = 一个稳定 media 实例"。复用策略细节后置。
3. **Command 层只包入口不重写内部**：先加 `Commands` 对象包裹现有 split/trim/move/delete 逻辑，UI 处理器改调 `Commands.xxx()`，旧实现原封不动，避免"按钮→Command→Store→render"链路重写炸。
4. **PM 思维与工程实现必须分开**：常站在产品 10 楼（AI 理解素材/情绪识别/Skill/MCP），但施工队还在打地基（播放/时间轴/数据模型/Command）。不混谈。
5. **OpenCut 正确用法**：研究它"解决什么问题 / 为什么这样设计 / 我们哪里对应 / 哪里不能复制"，**不抄 React 组件代码**。真正该抄的是数据模型 / 状态管理 / 播放控制 / Command 思想。

---

## 二、目标与验收（先定标准，再动手）

**改完后必须满足：**
1. 播放中 split 一个 clip → 无 AbortError，播放头无缝跨过切点继续走。
2. 同轨两段音频连续播 → 第二段从自身 `src_start` 起播，不是从素材 0s。
3. 播放中拖 zoom / 滚动条 → 不丢声、不重建媒体、无 canplay 风暴。
4. 0.5s 轮询在播放中 → 播放头照走，媒体元素身份不变。
5. 未来 Agent 调 `splitClip` 时，正在播放的预览不被打断。

**硬不变量（任何代码不得破坏）：**
- `I1` 每个 clip：`timelineEnd = start + duration`；`sourceEnd = src_start + duration`（无变速前提）；`0 ≤ src_start` 且 `src_start + duration ≤ asset.duration`。
- `I2` 媒体元素按 `key = clip.id`（**第一版**，phase 1 求"不死"）复用；active 期间**永不重建 / 永不 `load()` 杀害**。→ phase 2 升级为 `media identity = material_id + src_start + src_end + effect_state`（见六·明确不做），届时删 clip/重 split 不使缓存全失效。
- `I3` `isPlaying` 仍是唯一播放状态源；除 `playAllMedia` 外，任何代码不得 `play()` / 设全局 `muted`。
- `I4` 所有时间换算经 `timelineToSource()`，禁止散写 `us - seg.start`。

---

## 三、分步实施（Phase 0 已完 → Step1 → Step2 → Step3）

### Phase 0 媒体调用地图 ✅ 已完成（见〇）
只读审计，未改代码。交付物即上方地图。

### Step 1 — 媒体生命周期收口（播放期间媒体不死）⚠️ 根因

**铁律（"禁止修改区"）**：`isPlaying===true` 期间只允许 `seek/play/pause/volume`；**禁止** `render/rebuild/destroy/create/改 src/load()`。即播放期媒体元素身份不变。

**执行纪律（GPT 三次校准硬约束，本轮只许动 Step1，禁止越界）**：
- 本轮只允许修改 Step 1 涉及的 3 个动作。
- **禁止**：① 重构 `renderPreview` ② 新建 Player 模块 ③ 修改数据结构（SCHEMA/segment 字段）④ 修改 `main.py` ⑤ 修改 Command ⑥ 修改 UI 布局/交互。
- 只做三个动作：**A.** `startPlay` 增加播放窗口媒体准备；**B.** `playTick` 跨段禁调 `renderPreview`，改调 `seekActiveMediaToPlayhead`；**C.** 删除 canplay 自动 `play`。
- 完成后：① 输出修改文件位置 ② 输出修改前后逻辑 ③ 抽内联 JS 跑 `node --check`，不通过不准继续 ④ 等用户真机验证后再进 Step2。

**分三轮落地（GPT 要求：一刀一刀砍，每轮独立测试，绝不三刀同改）**：
- **Round A（先删 canplay）**：只删 L2793-2795 canplay 委托里的 `t.play()`，`play()` 收归 `playAllMedia()` 唯一入口。→ 重启 `start.bat` 真机验：起播/跨段不出现双 play 竞态、无 AbortError 加剧。验收过再进 B。
- **Round B（跨段不 render）**：L1544 跨段分支把 `renderPreview()` 换成 `seekActiveMediaToPlayhead()` —— 只对已存在活动元素调 `_seekMedia` 重新定位（经 I4 换算），**绝不创建/销毁/改 src/重跑 render**。→ 验：跨段无缝、媒体身份不变。验收过再进 C。
- **Round C（窗口化预建）**：`startPlay` 播放前新增 `ensureCurrentPlaybackWindowReady(playheadUs)`（**非一次性全量预建**），见下。

**安全最小改法（窗口化，比 v2.1 的"全量预建"更稳）**：
1. **`ensureCurrentPlaybackWindowReady(playheadUs)`**（替代 v2.1 的 `ensureAllMediaReady`）：只预建**当前播放点附近窗口**内的媒体，不遍历全部 clip。逻辑：取 `playheadUs` 前后各约一个窗口（如 ±10s 现载、再 +10s 预取）；对窗口内每条轨道确保 `video:ti`/`audio:ti` 元素已建并就位（复用 `renderPreview` 创建逻辑但只建不销毁），`seek` 到各自 `src_start`。这是播放期外唯一允许的"创建"。→ 避免 1000 素材时"点播放卡 5 秒"。
2. **L1544 跨段分支**（Round B）：见上，`seekActiveMediaToPlayhead` 只重定位不重建。
3. **删 canplay 自动 play**（Round A）：见上。

**播放调试日志（定位未来 bug 谁重建了媒体）**：新增 `const PLAYER_DEBUG=false`；在 `playTick` 跨段、`playAllMedia`、`renderPreview` 入口打结构化日志：
```
[PLAYER] t=5.20s active=audio:1 src=12.5s
[MEDIA]  audio:1 el=<id> created=false
[RENDER] renderPreview called=false
```
未来播放卡顿时，一眼看出是不是有人重建了媒体。

→ 三步全验：播放中跨段、拖 zoom、拖滚动条，媒体元素不重建、无 AbortError；中间段（t=10 起播的音频）正常出声。

### Step 2 — 时间换算集中（消灭散落 `src_start` 计算）
- 新增纯函数：
  ```
  function timelineToSource(seg, us){
    const local = Math.max(0, us - seg.start);
    const s0 = seg.src_start||0, s1 = seg.src_end||(s0+seg.duration);
    return Math.max(s0, Math.min(s1, s0 + local)) / 1e6;   // 秒，clamp 到源范围
  }
  function sourceToTimeline(seg, srcUs){ return seg.start + (srcUs - (seg.src_start||0)); }
  ```
- 路由：`_seekMedia`（L1138）、`drawAllWaves`（已修但应改为调函数）以及 **grep 全文件 `seg.start` 消费者**，统一走 `timelineToSource`。删掉各处手写的 `Math.max(0,(us-seg.start)/1e6)`。

→ 验证：split 后第二段从切开处播、波形连续、拉长段在源范围内可播。

**排序说明（GPT 三次校准备注）**：时间换算混乱（切开后第二段从头播）本质也是 timeline/source 混淆，Step2 甚至可考虑放 Step1 前。但当前最大痛点仍是卡顿（P-1 跨段重建），故维持 `Phase0→Step1→Step2→Step3` 顺序，Step2 紧接 Step1 验收后做。

**增量纪律（GPT 修正）**：不要一次 grep 全文件 `seg.start` 消费者全改。先只改 3 处并真机验：① `_seekMedia`（L1129，已含 src_start 换算，作基准）② 跨段续播定位（Step1 的 `seekActiveMediaToPlayhead`）③ `drawAllWaves` 波形映射。验证通过再扩展其余消费者。

### Step 3 — 内部 `Command` 入口包装（不重写内部）
```
const Commands = {
  splitClip(track, clipId, atUs): 调现有 split 逻辑（后端 tool / 现有前端处理函数）→ if(!isPlaying) renderPreview()
  trimClip(clipId, newStart, newDur): 调现有 trim 逻辑 → 落盘 → 最小渲染
  moveClip(clipId, newStart): 调现有 move 逻辑 → 落盘 → 最小渲染
  deleteClip(clipId): 调现有 delete 逻辑 → 落盘 → 最小渲染
}
```
- UI 处理器（split 按钮、拖拽手柄、trim 拉手、Del 键）改为调 `Commands.xxx()`，**旧实现原封不动裹在里面**。
- 本轮只把前端 UI 接过来验证 Command 封装不变量有效；**MCP 暴露 Command 是 P2，本轮不做**。

---

## 四、改动文件清单

| 文件 | 改动 | 风险 |
|---|---|---|
| `工作台v0.8时间轴.html` | Step1：灭 L1544 跨段重建 + 删 L2794 canplay play；Step2：新增 `timelineToSource/sourceToTimeline` + 路由消费者；Step3：新增 `Commands` + UI 处理器改调 | 中（核心路径，须语法校验+真机验） |
| `main.py` | 本轮**不改**（Command 复用现有 MCP 工具） | 低 |
| `工作台v0.8时间轴.html.bak` | 改前整文件复制备份 | — |

---

## 五、风险与回滚
- **回滚**：改前 `copy 工作台v0.8时间轴.html 工作台v0.8时间轴.html.bak`。任何一步验证不过，整体回退，不留半成品。
- **语法校验**：每改完一段，抽内联 JS 跑 `node --check`（本会话已验证流程可用），避免再出 `const` 重赋值崩 RAF 那种运行时雷。
- **真机验证必须重启 `start.bat`**（PyWebView 不热更新）——铁律，每次改完都提醒。
- **不一次全改**：Step1→验→Step2→验→Step3→验，每步独立可回退。

---

## 六、明确不做（deferred，底层未稳前禁止）
- ❌ 字幕 / 花字 / MG / 情绪识别等任何新功能
- ❌ 多视频叠加合成优化、音频混音增强
- ❌ MCP 接口、Skill 封装（等 Command 层稳了再做 P2）
- ❌ 特效原语引擎（之前记的 GPT 战略，后置）
- ❌ 媒体元素缓存池的精细复用策略（key 从 `clip.id` 升级到 `material_id+src_start+src_end`、变速/倒放预留）——Step1 先求"不死"，优化后置

---

## 七、执行确认（GPT 三次校准已拍板，待你最终放行 Step1）
- GPT 三次校准均已吸收：① v2.1 的 I1 speed 假设 / Step2 增量改 / Command 纯前端 / 调试日志；② v2.2 的窗口化预建 / 执行纪律 / 三轮分砍 / I2 phase2 / Step2 排序说明。
- **本会话二次读码发现（已在 v2.1 落地）**：renderPreview 已演进为按轨道复用元素，"裸删 L1544"会引入中间段媒体未创建的新 bug → Step1 用"窗口化预建 + 跨段只 seek 不 render"安全改法。
- **执行方式（GPT 硬要求）**：Step1 拆 A/B/C 三轮，每轮独立真机验；本轮只许动 Step1，禁止重构 renderPreview / 新建 Player / 改数据结构 / 改 main.py / 改 Command / 改 UI。
- **待你放行**：你回"走"或"第一轮走"，我即按 **Round A（删 canplay）→ 重启 `start.bat` 验 → Round B（跨段不 render）→ 验 → Round C（窗口化预建）→ 验** 的顺序执行，每轮抽内联 JS 跑 `node --check`、改完提醒你重启真机验。Step2/Step3 不碰。
