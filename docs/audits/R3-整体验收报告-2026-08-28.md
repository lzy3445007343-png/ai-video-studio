# R3 三层整体白盒验收报告（2026-08-28）

> 方法：r3-card-acceptance 闸。逐张抽 `⑥验收标准` → grep 真实代码取证（file:line）→ 二值判定：
> **pass**（有真实证据且语义匹配）/ **fail**（无证据或绷带）/ **需真机验收**（行为类 grep 只能证机制）/ **接口预留**（明确标注未启用）。
> 纪律：绝不信 commit message / 记忆 / 旧日志，以代码证据为准。三层共 **69 张卡**全跑。

## 一、总览

| 层 | 卡数 | done(架构确认) | 需真机验收 | 未达标(硬缺口) | 接口预留 |
|---|---|---|---|---|---|
| 层0-地基 | 12 | 5 (L0-06/07/08/09/10) | 7 | 0 | 0 |
| 层1-手感 | 24 | 0 | 24 | 0 | 0 |
| 层2-能力视觉 | 33 | 10 (L2-19/20/21/22/25/28/29/30/31/32) | 22 | 0 | 1 (L2-33) |
| **合计** | **69** | **15** | **53** | **0** | **1** |

**关键结论**：
1. **无"安静缩水"**——69 张卡全部有真实代码证据落地，没有"commit 写了完成、实际只交绷带/空提交"的情况（这正是 DRIFT 闸要防的）。
2. **0 张硬功能缺口（未达标）**——原 8 张未达标（L2-04/05/13/17/23/24/26/27）经第六节「DRIFT 复验纠正」逐张 grep 真实代码复核，全部核为已修/已落码（详见第六节），即三层 69 张卡「机制全部落地、无安静缩水」。
3. **53 张卡机制已落、行为待你真机点验**（层1 全 24 张 + 层0 的 7 张 + 层2 的 22 张）。
4. **1 张接口预留**（L2-33 降级横幅，Q11 未裁定，仅留组件接口未启用）。

## 二、未达标清单（0 张）

> 原第二节所列 6 张（L2-04/17/23/24/26/27）+ 后续并入的 L2-05/L2-13，共 **8 张**，已在第六节「DRIFT 复验纠正」中逐张 grep 真实代码复核，结论全部为「已修/已落码」，故本节现为空。
>
> 复验结论速览（详情见第六节）：
> - L2-24 / L2-26 / L2-27：原判定依据为写卡时陈旧快照，真实代码已落（误判→待真机验收）。
> - L2-04 / L2-17 / L2-23：真实后端缺口，已在 commit `5d40897` 修复（`sr`→`api` + 6 只读方法 / `duration` 字段 / `_refreshSeq`+`#516-guard`）。
> - L2-05 / L2-13：本会话（commit `990604a`）落地 Saved 预设 + 控制条音量/loop。

## 三、接口预留（1 张）

- **L2-33 降级横幅**：`degradedBanner` div `display:none`，`maybeShowDegradedBanner()` 直接 `return`（Q11 未裁定前不启用），所有分支判定逻辑未落地。符合卡⑧「不启用」裁定，非缺陷。

## 四、各层分布

### 层0-地基（12）：5 done / 7 需真机 / 0 未达标
- done（架构可静态确认）：L0-06 KF三方同步语义、L0-07 契约护栏、L0-08 clip-local KF 数据层、L0-09 enforceRules 管线、L0-10 MCP KF 工具。
- 需真机（含行为/视觉/性能类⑥）：L0-01 拖动刷新收窄、L0-02 keyed-diff、L0-03 共享预览态、L0-04 吸附引擎、L0-05 标尺虚拟化、L0-11 UI反馈层、L0-12 undo 纪律。
- 命名偏差（非阻塞，建议补注）：L0-09 实现为 `_run_enforce_pipeline` 而非字面 `enforce_rules`；L0-02 未加 keyed 路径 `if(!Store.state.drag)` 字面守卫，但目标由更上游机制达成。

### 层1-手感（24）：0 done / 24 需真机 / 0 未达标
- 全部 24 张核心机制已 grep 实证落码（translate3d 纵向跟随 / isShiftDisabled / pendingBox 框选 / _edgeScrollCtx 边缘滚 / .snapped 吸附实心态 / snapPreviewPosition 预览对齐线 / snapRotation 旋转吸附 / drag-transform 不打帧 / notifyPreviewConsumers 三方联动 / renderPreviewMaybe 即时seek / zoomPct 缩放 / previewCtxMenu 右键 / text-edit-overlay 双击文字 / ew-resize 光标 / scrub-session drag-to-scrub / ExprParse 表达式 / _kfDragActive KF 同步 / snapFrame 帧吸附 / drop-target-media 落点分支 / displayRowTopY 落点线）。
- 因层1 ⑥标准绝大多数是行为类（跟手/实时/不弹回/一致），机器只能证机制、证不了手感，故全标「需真机验收」。

### 层2-能力视觉（33）：10 done / 22 需真机 / 0 未达标 / 1 接口预留
- done（机制+无行为缺口）：L2-19 在线tab占位、L2-20 tab图标Tooltip、L2-21 视图持久化、L2-22 素材库右键、L2-25 书签、L2-28 KF菱形SVG、L2-29 reset统一、L2-30 顶栏、L2-31 名称截断、L2-32 遮罩模糊。
- 需真机（机制已落、视觉/交互待点）：L2-01/02/03/04/05/06/07/08/09/10/11/12/13/14/15/16/17/18/23/24/26/27（原 8 张未达标经 DRIFT 复验全部转入此处）。
- 未达标：0 张（见第二节 + 第六节）。
- 接口预留：L2-33。
- 偏离项（非阻塞）：L2-01 仅单写 `seg.blend_mode`、未做 `params["blend.mode"]` 双写（画面混合由 Video DSL 渲染器承担，卡⑧已声明边界）。

## 五、下一步建议

1. **原 8 张未达标已全部在第六节 DRIFT 复验中核为已修/已落码**——L2-04/17/23 后端缺口已在 `5d40897` 修复，L2-24/26/27 为写卡快照误判（真实代码已落），L2-05/13 本会话 `990604a` 落地。故「修未达标」一项已清零，无需再修代码。
2. **你做真机验收**：重启 `start.bat`，按"需真机"清单逐张点（层1 全 24 张是重点手感项）。
3. **xlsx 同步**：`R3 验收差距表` 前序（5d40897/48f5e88）已把 8 张统一回写为「代码已落·待真机验收」；本回合补同步 `验收总表` 的 L2-05（行64，原「待决·需L0-08 Q1裁决」）/L2-13（行44，原「待决·需Q9裁定」）→ 同改「代码已落·待真机验收」，与差距表 + 本报告顶表口径一致。

---
*生成方式：层0/层2a/层2b 由白盒验收子代理跑（grep 取证），层1 因子代理工具瞬错由主代理自跑（抽⑥标准+grep 实证）。所有结论以 `file:line` 代码证据为准。*

---

## 六、DRIFT 复验纠正（2026-08-27，按"真实代码为权威"重核 8 张未达标）

> 复验触发：落码前 grep 真实代码发现，原第二节「8 张未达标」中 **3 张其实已落码并提交**，原判定依据为卡片「①现状（grep 实测）」的**写卡时陈旧快照**，而非当前真实代码（典型 DRIFT：把"写卡时尚未实现"当成"现在没实现"）。已按纪律以真实代码为准重新裁定。

### 纠正结论（8 张）

| 卡 | 原判定 | 真实代码复核（file:line） | 修正 |
|---|---|---|---|
| **L2-24** 选中轨高亮 | 未达标 | `timeline.js:602-604`(keyed-diff 注入) + `:714-716`(全量注入) + `工作台v0.8时间轴.html:488-489` `.track.track-selected` CSS | **已落码**（误判→改为代码已落·待真机验收） |
| **L2-26** 按类型轨高 | 未达标 | `timeline.js:404-413` `trackHeightOf()` + `:433` 使用；`工作台v0.8时间轴.html:480-485` 逐类型 CSS + `:457-462` 标签 + `:485/:462` 6px 间距 | **已落码**（误判→待真机验收；`TRACK_H=48` 仅剩未使用回退常量，注释已澄清） |
| **L2-27** KF 40px 门限 | 未达标 | `timeline.js:526-529` `if (segWidthPx < 40) continue;` | **已落码**（误判→待真机验收；其余 10 子项按真机核验） |
| **L2-04** Graphic/get_track_text | 未达标（真缺口） | `mcp_server.py:383/390/398/404/421/430` 用未定义 `sr.`（应 `api.`）；`main.py` 缺 `list_tracks/get_track_text/get_segment_detail/get_effects/get_material_peaks/get_segment_peaks` 6 方法 | **已修**：`sr`→`api` + `main.py` 实现 6 个只读方法（纯读，M1 安全线） |
| **L2-17** 时长徽标数据源 | 未达标（真缺口） | CSS 徽标 `工作台v0.8时间轴.html:158-160` 已落；`main.py` `import_media_by_paths` 入仓项原无 `duration` | **已修**：`main.py` 入仓项加 `"duration": duration_for(dst, mtype)`（加性字段，微秒） |
| **L2-23** 拖素材清空 bug | 未达标（真缺口） | `refresh()` 原有 RefreshLock 等多重守卫，但缺空稿防御 + 乱序串行化 | **已修**：`工作台v0.8时间轴.html` 加 `_refreshSeq` 串行化 + `#516-guard` 空稿防御（A-1/A-2，仅挡"读盘竞态半写文件"，合法空库不受影响） |
| **L2-05** Graph Editor 缓动曲线 | 未达标 | 2026-08-27 落码：`property/graph-editor.js` 新增 Saved tab（localStorage `avs.graph-presets` 保存/删除 + 缩略曲线）；补 flat 零跨度原因 | **已落码**：曲线保存/删除闭环，转「需真机验收」 |
| **L2-13** 控制条音量/loop | 未达标 | 2026-08-27 落码：Q9 按分支 A 裁定实现——`volumeSlider`+`volumeBtn`+`loopBtn` 控制条；`PlayerManager.setGlobalVolume`/`AudioEngine.setGlobalVolume`/`Store.state.loopOn`(localStorage)；时间码 `HH:MM:SS:FF` 前序已落 | **已落码**：音量/loop 闭环，转「需真机验收」 |

### 本轮实际落码（commit 待 push）
- `mcp_server.py`：6 处 `sr.`→`api.`（list_tracks/get_track_text/get_segment_detail/get_effects/get_material_peaks/get_segment_peaks）
- `main.py`：新增 6 个只读方法 + `import_media_by_paths` 物料加 `duration`
- `工作台v0.8时间轴.html`：`refresh()` 加 `_refreshSeq` 串行化 + `#516-guard` 空稿防御；`TRACK_H` 注释澄清
- 校验：`py_compile main.py mcp_server.py` 通过；`node --check` 抽 `refresh()` 通过

### 纪律教训（固化）
- 验收闸必须以**当前真实代码** grep 为权威，禁止信任卡片「①现状」快照（写卡时状态≠现在）。本次 3 张误判即源于此。
- 落码/定性前必 grep 真实证据（DRIFT 纪律），不靠记忆/commit message/卡片快照。

