# P7 路C编辑逻辑 · 执行路线图（铺路版 2026-08-30）

> 目的：把 P7 剩下的路铺平，使未来会话（或用户回来）**无需重新侦察**即可逐片执行。
> 纪律：每片「从头做穿：预览可见 → 落库正确 → 导出剪映仍存在 → 参数面板可写回 → MCP 可调用」。
> 红线（改任何一片都守）：不动 DOM id / 脚本加载顺序 / draft_content.json 结构 / Command 契约 / KF 菱形 / 剪映导出+MCP 入口。
> 全量台账 fresh（2026-08-29）：800 = 605 完成 / 21 进行中 / 174 待办。

---

## 〇、本回合已落地（用户批「走B直接修」）

| 修复 | 文件:行 | 真因 | 状态 |
|---|---|---|---|
| **F1** 拖入回弹（B-01 行错位） | `工作台v0.8时间轴.html:3509` | dragover 命中可绑定段时 `__libDropCache.drop=null` → drop 回退 `computeDrop(...,null)` → 间隙恒归上方轨，与预览错位 | 已改 + 语法眼检通过，**待你重启 start.bat 真机验** |
| **F2** 拖入回弹（B-01 时间右推） | `main.py:3714` | 已有轨落点「段长溢出到下一段」→ 旧 `_free_start_on_track` 右推 = 段弹到右边 | 已改 + `py_compile` 通过，**待真机验**；仅 drop 路径，paste/relocate 右推安全网保留 |

**F2 关键保证**：`_free_start_on_track` 仍被 paste(4770/4921)、relocate(5265) 直接调用，本改只动 `add_to_timeline_impl` 的 `track_tid` 重叠分支 → 新建同类型轨（对齐 OpenCut `resolveTrackPlacement` 永不右推），不影响复制/移动。

---

## 一、P7 垂直切片地图（每片独立可验）

> 切片顺序 = 依赖序（先底层建轨，再 KF 三路，最后回弹收尾）。每片改完必 `node --check` + grep 牵连处 + 双验证闸。

### 切片 1 · #810 建轨语义（底层已落，待真机）
- 真身：`timeline.js:269-377` `resolveNewDrop` + `computeDrop`（占用/不兼容→新建同类型轨，对齐 OpenCut）
- 落库：`main.py:3681-3703` `track_tid`/`insert_index` 分支；`_insert_track`(543)
- 验收闸：拖视频到音频轨→自动建视频覆盖轨且段落对位置；拖贴纸到视频轨→建贴纸轨夹中间；**导出剪映草稿仍存在**。

### 切片 2 · #807 位置 X/Y 双轴 KF（码已落，待真机双验证）
- 真身：`preview-drag.js:151-178` `add_keyframe(localUs)`（拖位置同步播放头 X/Y KF）
- 牵连：`kf-panel.js` X/Y ◆ 行；`transform-handle.js:423` `localSnap`
- 验收闸：打 X/Y 双 KF → 拖动段 → 两轴 KF 都更新（**不回弹、不丢一轴**）；导出剪映 KF 仍在。

### 切片 3 · #806 缩放/旋转拖拽 KF（码已落，待真机双验证）
- 真身：`transform-handle.js:286-318`(Resize) / `358-379`(Rotate) `add_keyframe`
- 验收闸：打 Scale/Rotation KF → 画布缩放/旋转 → 对应 KF 更新；导出剪映仍在。

### 切片 4 · #808 参数面板 KF 写回对称（码已落，待真机双验证）
- 真身：`kf-panel.js:176-228`(Scale 锁定行 blur 已修) / `351-353`(普通行)
- 验收闸：面板改值→播放头 KF 同步；X/Y/Scale/Rotation 四通道写回对称无遗漏。

### 切片 5 · #809 拖入回弹（F1+F2 已修，待真机）
- 见「〇」。验收闸：拖素材到间隙（上方/下方）→ 落点与预览影子段**完全一致**（不归错行、不右推）；窄间隙放长片→新建同类型轨而非弹位。

---

## 二、双验证闸铁律（路C 不可省）
1. **预览看得见**：前端拖拽/面板操作实时反映到画布+时间轴。
2. **导出剪映草稿仍存在**：`python -c "from pyJianYingDraft import..."` 或项目导出脚本跑通，KF/轨/段不丢。
两闸任一不过 = 该片**未达标**，禁止标 done。

---

## 三、非 P7 真功能缺口（另线，铺路时一并列出，避免回来迷路）
| 任务 | 缺口 | 备注 |
|---|---|---|
| #370 | 特效轨导出 | 特效落库已通，导出剪映未接 |
| #479 | 播放器素材拖不动 | 播放器内拖拽未接 timeline |
| #516 | 拖素材清空双清空 | 拖入清空旧段 bug |
| #181/#191-194 | 遮罩 / ASR / 贴纸 | 资产已部分落，编辑逻辑未通 |
| #111/#113/#115 | 手感 / 吸附 / 右键菜单 | 播放器行为圣经已研，未落地 |
| #797-800 | UI P4 尺寸%化 / P5 播放器规格 | 换肤 P0 已完成，P4/P5 待做 |

---

## 四、台账清理建议（stale 项，仅动任务表不动码）
- **#500** ⑧-A 空工程批首回滚：已被 #510 验收通过取代 → 可关单
- **#802** Wave2 #B-01+#B-04 复诊：#B-04 已落码 → stale，可关单
- **#112/#115/#117/#157-159/#169/#182/#183** 等功能已落却标 pending → 批量关单（需你确认）
- **#663-679** 同批 L1 切片拆成多套重复规划 → 合并

---

## 五、你回来后的执行序（一键可续）
1. **重启 `start.bat`**，拖素材入时间轴验 F1+F2（看回弹是否消失）。
2. 切片 1→5 逐片**真机双验证**（码基本已落，主要验手感+导出）。
3. 任一片不过闸 → 按「先侦察再改」纪律 grep 牵连处后小步改。
4. 台账清理（第四节）与我确认后批量关单。
5. 非 P7 缺口（第三节）另开线，不混进 P7。

> 当前 P7 编辑逻辑**码已齐 80%**，卡在「真机双验证 + F1/F2 回弹收尾」。你无需盯盘，回来按序验即可。
