# 明日计划：OpenCut 全面对齐（2026-08-16）

> 昨晚（08-15）完成：架构收口 5 步 + Command 层 + PropertiesPanel 路由 + 标尺初步调整 + 分隔条。
> 今日目标：把 OpenCut 能拿的全拿了——按 P0→P1→P2 顺序，每项都有 OpenCut 源码锚点。

---

## ✅ 待办清单（可勾选，做完一项勾一项）

### P0（必须，先做）
- [ ] **标尺两级刻度**（用户点名）——重写 timeline.js rulerStep/rulerLabel 为 label+tick 双间隔 + 虚拟化渲染（OpenCut: timeline-ruler.tsx / ruler-utils.ts / timeline-tick.tsx）
- [ ] **trim 补素材手感**（真 bug）——前端拖拽到后端参数链路排查

### P1（应该，对齐交互）
- [ ] 右键菜单（OpenCut: preview/components/context-menu.tsx）
- [ ] 段级静音/隐藏（toggle-elements-muted/visibility-selected）
- [ ] 轨道锁定（TrackLabelsPanel lock）
- [ ] 资产面板 grid/list 切换（assets.tsx line 356-362）
- [ ] audio/speed tab 实装（现有 toggle_track_mute + set_segment_speed 接入 PropertiesPanel 占位）

### P2（可选，看时间）
- [ ] 特效原语引擎设计稿（参考 OpenCut effects/types.ts `Effect {id,type,params,enabled}`）
- [ ] 多进程撤销互踩（MCP 与桌面并发 undo）
- [ ] 素材库加载异常排查
- [ ] C.0 AudioEngine 设计稿细化（昨晚对比结论：Web Audio lookahead 调度是音频根治路径）

---

## 昨晚新增情报（播放器冻结项 vs OpenCut 深度对比，`docs/audits/playback-freeze-vs-opencut.md`）

- **能借鉴**：Web Audio lookahead 调度（根治跨段无声）、sessionId 防竞态、单一时间源、stop 清场重建
- **不能借鉴**：canvas 逐帧（工程大+导出路径不同）、Rust/wasm、React、多场景
- **结论**：音频迁 Web Audio 是唯一根治路径；视频元素模式够用

## 明日起手顺序

```
1. 标尺两级刻度（用户点名，P0）→ 2. trim 补素材（真 bug，P0）
→ 3. 右键菜单 + 段级静音隐藏 + 轨道锁定（P1 三件套）
→ 4. 资产面板 grid/list（P1）→ 5. audio/speed tab（P1）
→ 6. 特效原语引擎设计稿（P2）→ 7. 多进程撤销互踩（P2）
```

## 铁律提醒
- 每步 commit + 桌面日志记录（`C:\Users\34450\Desktop\AI剪辑工作台_开发日志.md`）
- 改 HTML 必须重启 start.bat 验证
- 回归基线 REGRESSION.md 跑一遍
- 播放器冻结项不动（归 C.0）
