# 雷点清单 v0.1（活文档，随审查持续追加）

> 日期：2026-08-22 21:20 · 全面审查产物④（v0.1 起步版）
> 定位：**能力矩阵答"缺什么"，本清单答"藏着什么会炸"**——所有代码层面的隐藏风险/结构性雷，不分模块，按严重度排
> 状态图例：🔍 静态疑似 → 🧪 动态验证中 → ✅ 已确诊 → 🔧 已修复（需回归）→ 🔒 待 GPT 评审
> 每轮审计出矩阵的同时，把该模块的雷追加进来

---

## 一、P0 ① 状态流审计已发现（本轮）

| # | 雷点 | 现象/风险 | 严重度 | 状态 |
|---|---|---|---|---|
| C1 | **◆ 打点实时读播放头（未锁）** | toggleKf 每次点击 `TimelineMapper.playheadLocal(s)` 实时取——两次点击间播放头微动 → X/Y 落到不同时间 → **边用边产生错位数据** | 🔴 高 | ✅ **不成立**（2026-08-22 21:25 复现：两次 toggleKf playheadUs 相同=1691666.67，X/Y t=1699983 同点。撤回破例修） |
| D1 | **KF 拖拽锁 _kfDragActive 无释放兜底** | 裸全局变量，只有 mouseup 释放；鼠标移出窗口/pointercancel/异常路径 → 锁永挂 → refresh 永久跳过 → **UI 卡死** | 🔴 高 | ✅ **本次未复现**（lock/unlock 两两配对；保留为异常路径风险，**不立即修**，但要加 pointercancel 兜底时一起补） |
| A1 | **拖时间轴段每帧全量重建** | `_scheduleDragRender→_emit→renderAll→renderTimeline 全量销毁重建 DOM`——段多/素材多必卡，**UI 跟手性差** | 🟠 中 | 🔍 静态已定 |
| B1 | **renderAll 双跑** | `Store.subscribe(renderAll)`(HTML:1254) + refresh 尾部显式 `renderAll`(HTML:3253)——每次 refresh 全量渲染 ×2 | 🟠 中 | 🔍 静态已定 |
| E1 | **500ms 轮询全量重建** | 无操作也每 0.5s get_state + 全量 renderAll（含素材库/时间轴 DOM 重建）——持续 CPU 占用，素材消失 B12 的触发环境 | 🟠 中 | 🔍 静态已定 |

## 二、Backlog 已登记（完整分步计划-2026-08-20.md）

| # | 雷点 | 现象 | 严重度 | 状态 |
|---|---|---|---|---|
| B10 | **trim 无源段拉长缩短错乱** | 图片/MP3 左拖弹回、左边界往右拖整段移动；前端 isMedia 用轨类型(main 轨恒 video)，后端 left_limit 挡死负 delta；曾试修(34a7856)回滚(3a4474a) | 🔴 高 | 🔒 待审（P1 ④ 时间线） |
| B11 | **视频左边界往右拖变"移动段"** | handle 命中/渲染问题 | 🟠 中 | 🔒 待审（P1 ④） |
| B12 | **素材库"全消失"复发** | 拖 KF/拖预览/拖素材后素材卡片全空；FIX-1(传参)+防御已加，真根因未除（疑与 E1 轮询+renderMedia 异常相关） | 🔴 高 | 🔍 待审（P0 ① 动态） |
| B13 | **KF 组拖动"复制"现象** | 选中两个拖 → 原地留一个+拖出一个；已修 3 层（合并/容差/锁定），残留疑与 A3 视觉残留相关 | 🟠 中 | 🔍 待审 |

## 三、已知结构性风险（代码直接可见，无需复现）

| # | 雷点 | 位置 | 风险 | 严重度 | 状态 |
|---|---|---|---|---|---|
| S1 | **导入剪映草稿同步 ffmpeg 转码** | main.py `_prepare_preview_media` | HEVC/MOV 逐个全量转码、**同步阻塞 UI**——导一个多视频草稿冻结数分钟（已发生：8/21 卡死事件） | 🔴 高 | 🔒 待审（P1 ⑥ 媒体） |
| S2 | **previewState.visualEls stale key 残留** | renderer.js:142 rec.key 无清理路径 | 段删/折叠后残留 → 拖动 commit 用旧 key → 无效 add_keyframe（FIX-2 只加了防御没清根源） | 🟠 中 | 🔒 待审 |
| S3 | **draft_state.json 空保存覆盖** | save_state 时机 | 导入剪映中断/异常路径可能把空 draft 落盘（8/21 素材"丢失"事件根因之一） | 🟠 中 | 🔒 待审 |
| S4 | **时间精度 float 微秒** | 全局 playheadUs | 浮点误差累积 → 帧对齐不准；新版用 MediaTime 整数 tick（A2 帧吸附已缓解未根治） | 🟠 中 | 🔒 待审（P2 ⑦） |
| S5 | **30fps 硬编码** | KF 帧吸附 33333us 等 | 换帧率项目（25/60fps）全错位 | 🟡 低 | 🔒 待审 |
| S6 | **导入素材不去重（D7）** | import_media_by_paths/drop_files 每次导入都 copy_to_assets，同名素材反复导入复制多份——粗剪_IMG_4379 被复制 12 份（1.2G），C 盘 98% 满元凶之一。8/22 已手动清理 13 副本，代码未修 | 🟠 中 | 🔒 待审（P1 ⑥ 媒体） |

## 四、审计规划中的雷（P0②-P2 待查清单）

| 模块 | 待查雷点方向 |
|---|---|
| P0 ② 关键帧 | 扁平 keys vs bindings+channels 数据模型错位、组联动缺失、marker 视觉残留 |
| P0 ③ 画布 | 坐标五层换算（screen→viewport→canvas→element→transform）、命中系统（看到 A 点到 B） |
| P1 ④ 时间线 | trim 轨类型误判(B10)、handle 命中(B11)、操作组合雷 |
| P1 ⑤ 播放器 | 三时间（playhead/media/local）混用、播放驱动状态 |
| P1 ⑥ 媒体 | 同步转码(S1)、缩略图/波形性能、MP4 落轨 |
| P2 ⑦ 数据模型 | 逐字段对比 segment/transform/animations（最大未知） |
| P2 ⑧⑨⑩ | Undo 一致性、导出坐标、MCP 接口复核 |

---

## 修复优先级原则（GPT 定案）

1. **确诊且会持续污染数据的**（如 C1）→ 立刻修，不等统一阶段
2. **已确诊结构性雷** → 随对应模块审计完一起修
3. **功能缺失** → 审查全部完成后统一按路线图补（能力矩阵）

*记录：WorkBuddy · 2026-08-22 21:20 · v0.1 · 后续每轮审计追加新雷点，修复后更新状态*
