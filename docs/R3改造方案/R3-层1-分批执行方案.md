# R3 层1 分批执行方案（交付批次）

> 来源：基于 `R3-层1-落地方案.md`（执行设计）+ `层1-手感/00-层1总览.md`。
> 本文 = **分批交付计划**（给执行用的），不重复设计细节，只定"分几批、每批哪几张、怎么验收、牵连红线在哪"。
> 红线纪律：L0-07 契约禁动表（Api / draft_state.json / seg_id / track_type 枚举）全程不碰。

---

## 0. 关键前置结论：所有待决已按 A 分支裁定 → 24 张全部解锁

2026-08-27 用户裁定：层1 全部待决点**统一走 A 分支**（代价小、不碰红线）。逐项收口如下：

| 待决点 | 裁定 | 影响卡 |
|---|---|---|
| Q3 Player 拖动打帧 | **A**：只改 base transform、不打帧（R-VALUE-NOFRAME） | L1-09 |
| Q4 Shift 语义 | **A**：删锁比例，全局 Shift=禁吸附 | L1-08（L1-02/06/07 已按禁吸附落地） |
| Q5 画布缩放键位 | **A**：Ctrl/Meta+滚轮=画布缩放 | L1-12 |
| L1-14 文字写命令 | **A**：扩现有命令参数加 `text`，不碰 Api 签名 | L1-14 |
| L1-08 通道清除 | **A**：前端 `delete` 清 scale 通道（专项验证 refresh 回填"复活"，若复活改 B 或加守卫） | L1-08 |
| Q14 阈值 | **A**：统一 5px（`store.js THRESHOLD` 一并提 5） | L1-03 / L1-22 |
| Q6 seek 节流 | **A 默认**：rAF 合并去 80ms 节流 | L1-11 |
| Q17 拖动阈值 | 随 L0-04 落码一并裁定（常量收口） | L1-16 |

→ 无硬阻塞，3 批可连续推进。

---

## 1. 分批总表（3 批 × 8 张，每批独立验收）

| 批次 | 主题 | 卡 | 前置（均已在层0 收口） |
|---|---|---|---|
| **Batch 1** | 渲染手感 + 纯前端基础 | L1-01 / 03 / 04 / 11 / 15 / 16 / 18 / 19 | L0-01 / 02（渲染收窄） |
| **Batch 2** | 吸附引擎消费 + UI 组件 | L1-02 / 05 / 06 / 07 / 13 / 22 / 23 / 24 | L0-04（吸附引擎）、L0-11（右键菜单） |
| **Batch 3** | preview 态 + 缩放 + 文字 + KF 联动 | L1-08 / 09 / 10 / 12 / 14 / 17 / 20 / 21 | L0-03（预览态）、L0-06（KF 语义）、L0-08（KF 数据层） |

---

## 2. 各批执行要点

### Batch 1（8 · 渲染手感根治 + 输入框基础 · 零裁决 · 立即开工）
用户最大痛点（卡顿/手感缺失）集中在这批，且全不依赖任何待决。

| 卡 | 改动文件 | 验收门 | 红线 |
|---|---|---|---|
| L1-01 跨轨纵向落轨跟随 | 工作台.html `startPointer`/`onPointerMove` + L0-01 `renderDragOverlay` | 跨轨拖动段 DOM transform 逐帧一致；resize 不跟随 | Api/draft 不碰 |
| L1-03 框选实时 | 工作台.html `onBoxMove`/`onBoxUp` + 轮询守卫 `pendingBox` | 选区实时增减；松手恰 1 次 `Store.set` | `pendingBox` 临时态不落盘 |
| L1-04 边缘滚动 | 工作台.html 新增 `EDGE_SCROLL` rAF 内核，挂三路 | 边缘 100px 自动滚；单帧≤15px；mouseup 后 rAF 停 | player.js 不碰 |
| L1-11 即时 seek | 工作台.html `scheduleScrubPreview` 80ms→rAF 合并 | 拖动中 Player 每帧更新无滞后；`renderAll` 拖动中 0 次 | player.js 不碰 |
| L1-15 resize 光标 | `property/transform-handle.js` `getResizeCursor` | 各手柄方向光标正确；旋转随角度变 | 纯 CSS/JS |
| L1-16 手柄细节 | `transform-handle.js` 9→8 点；命中区视觉/命中分离 | 8 点；视觉外 3px 命中；高缩放手感一致 | 阈值常量挂 L0-04 |
| L1-18 聚焦全选 | `property/NumberField.js` `bind` + 各 panel 补齐 | 点击全文选中；Enter/blur 提交 | `PropertyDraft` 签名不碰 |
| L1-19 数学表达式 | 新增 `property/expr-parse.js`（**禁 eval**）+ `NumberField._parse` | `1920/2`→960；非法不采纳 | Api 不碰 |

**同文件合并验收**：L1-15+L1-16 同改 `transform-handle.js` → 同批同验；L1-18+L1-19 同改 `NumberField.js` → 同批同验（L1-17 的 ScrubSession 也改 NumberField，归 Batch 3，在 18/19 铺好基础上追加，顺序不冲突）。

### Batch 2（8 · 吸附引擎消费 + UI 组件 · 依赖 L0-04 / L0-11）

| 卡 | 改动文件 | 验收门 | 红线 |
|---|---|---|---|
| L1-02 clipResizeShift 禁用吸附 | 工作台.html 三路径加 `!SnapEngine.isShiftDisabled(e)` | Shift 全程无 snap-guide；grep 无散落 `e.shiftKey` | playhead 语义不变 |
| L1-05 吸附实心态 | 工作台.html `.playhead-handle` + `onPhMove` 挂 `setPlayheadSnapped` | 吸附命中手柄实心；松手恢复 | player.js 不碰 |
| L1-06 预览对齐线 + 位置吸附 | `property/preview-drag.js` 接 `SnapEngine.snapPreviewPosition` + 新增 `#previewSnapGuides` | ≤8 屏像素出 1px 贯穿线并吸附；松手无残留 | Api 不碰 |
| L1-07 旋转吸附 | `transform-handle.js RotateSession` 接 `SnapEngine.snapRotation` | ±5° 吸整 90°；Shift 可停 87/93° | 角度域不外规整 |
| L1-22 kfGraph 帧吸附 | 工作台.html `onKfDiamondDown` 接 `TimelineMapper.snapFrame` | 落库 `t%frameUs==0`；短段 clamp | Api 不碰 |
| L1-24 snapFrame 接线 | 工作台.html dragover/drop 抽 `dropTimeUs()` 接 `snapFrame` | `start%frameUs==0`；预览=实际 | Api 不碰 |
| L1-13 预览右键菜单 | 工作台.html `previewStack` 绑 contextmenu→L0-11 `openContextMenu` | 右键弹菜单；Fit→zoomPct100%；Esc 关 | Api 不碰 |
| L1-23 落点高亮分支 | 工作台.html `onTimelineDragOver` 命中元素 `opacity-50` 分支 | 拖到元素 opacity.5 且其他指示全隐；三态互斥 | 媒体替换归 L2-18 |

### Batch 3（8 · preview 态 + 缩放 + 文字 + KF 联动 · 待决已按 A 裁定）

| 卡 | 改动文件 | 验收门 | 红线 / 裁定 |
|---|---|---|---|
| L1-08 缩放吸附与清通道 | `transform-handle.js ResizeSession` 接 `snapScale`；`MIN_SCALE` 0.05→0.01；删锁比例(Q4-A)；清 scale 通道(Q4-A 前端 delete) | 整数比例吸附；清通道后动画不覆盖；下限 0.01 | 通道清除走 A，专项验证 refresh 复活 |
| L1-09 Player 拖动不打帧 | `preview-drag.js DragSession.commit` 删 `add_keyframe` 分支走 base(Q3-A) | 有通道段拖后 keys 不变；undo+1 | 联动 L0-03 ③3.2 分流语义(Q3-A) |
| L1-10 拖中三方联动 | 三路径接 L0-03 `notifyPreviewConsumers`/`getPreviewSeg` | 三向实时；松手 1 提交 1 undo；Esc 回落 | Api 不碰 |
| L1-12 画布缩放按键 | `viewport-input.js` + 工作台.html `tlScroll` wheel(Q5-A) | Ctrl+滚轮画布缩放生效；title 一致 | Api 不碰 |
| L1-14 双击文字编辑 | `renderer.js text-layer` + `preview-drag.js` 绑 dblclick(L1-14-A) | 双击编辑；输入实时；blur 提交 undo+1 | **A 扩现有命令参数加 text 不碰 Api**（B 才碰红线，已否） |
| L1-17 dragToScrub | `NumberField.js` icon + 新增 `ScrubSession`(interaction-kernel.js) | icon 拖动实时；Player 实时；正式数据不变；undo+1 | 遵循 R-VALUE（有通道范围→打帧） |
| L1-20 effect 实时预览 | `effect-panel.js` `onPreview`→overlay + `onCommit` 去 debounce | 拖动中画面实时；一次 `update_effect` | Api 不碰 |
| L1-21 KF 标记拖动中同步 | 工作台.html `startKfMarkerDrag` + `kf-panel.js` + 轮询守卫 `_kfDragActive` | 拖 marker ◆实时；输入→marker 实时；拖动中 0 次 update；松手 1 undo | Api 不碰 |

---

## 3. 牵连红线速查（落码每批 grep 复核）

- **L0-01 局部渲染入口**：工作台.html `_scheduleDragRender`/`renderDragOverlay`（L1-01/03/04/11/21/23 消费，不得回退全局 `_emit`）。
- **L0-03 preview 接口**：`previewOverlay`/`getPreviewSeg`/`notifyPreviewConsumers`/`applyPreview`/`commitPreview`（L1-08/09/10/14/17/20/21 消费；写方/读方统一）。
- **L0-04 SnapEngine**：`isShiftDisabled`/`resolve`/`snapPreviewPosition`/`snapRotation`/`snapScale` + `TimelineMapper.snapFrame`（L1-02/05/06/07/08/22/23/24 消费）。
- **L0-06 KF 门控**：`isPlayheadWithinRange`/`KF_HIT_TOLERANCE_US`（L1-17/21 消费）。
- **2s 轮询守卫**：工作台.html 须扩 `!pendingBox`(L1-03) + `!_kfDragActive`(L1-21) + `!previewActive()`(L0-03)。
- **`alert(` 15 处**：L1-13/L1-23 的 coming soon/右键反馈必须走 L0-11 toast，不得新增 `alert(`。
- **Api 红线唯一触点**：L1-14 仅分支 B 碰 Api（已否，走 A）。

---

## 4. 已知遗留 gap（用户 message1 报的"同一轨道两素材吸附不了"）

- 现象：同轨相邻 clip 之间无法互吸（当前 `SnapEngine` 候选点集 = 播放头 / 轨边 / 关键帧，未含同轨相邻 clip 边缘）。
- 处置：**不在本批方案扩写范围**（用户原话"后面再说"）。建议 Batch 2 落吸附卡时顺带评估——是否把"同轨相邻 clip 边缘"纳入 `SnapEngine` 候选点集（低成本扩展）；若评估为独立改动，单列 follow-up，不阻塞 Batch 2。
- 记录为 R3 已知 TODO，等用户确认是否纳入。

---

## 5. 层1 算"落完"的验收门控

- 层0 八张硬门控（L0-01/02/03/04/05/06/08/11）验收全绿（层0 已 commit `ef1273a` + 缩放热修复 `065770f`）；
- 24 张卡逐张达到 §2 验收门（含 R-VALUE / R-SEEK / R-VALUE-NOFRAME 三法则在 L1-09/11/17/20/21 落地一致）；
- 全部待决已按 A 收口（§0 表）；
- 跨层依赖闭合：L1-09/10/21/22 消费 L0-06+L0-08 一致；L1-06/23/24 消费 L0-04 一致；
- L0-07 护栏：层1 全部提交零触碰 C-1~C-8（L1-14 走 A，未碰 Api）；
- 被层2 依赖的卡先落：L1-17（L2-09）、L1-23（L2-18）、L1-21（L2 能力卡）。
