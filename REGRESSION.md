# 回归验收基线（Regression Test Baseline）

> 日期：2026-08-15
> 目的：每次改动后跑一遍，回答「这次是进步还是倒退」——不再凭感觉。
> 用法：改完代码 → 重开 start.bat → 按下面清单逐项测 → 全部 ✅ = 通过；任何 ❌ = 记录现象+控制台日志，定位后再改。

---

## 0. 快速检查（每次必做，30 秒）

| # | 操作 | 通过标准 |
|---|------|---------|
| R0-1 | 重开 start.bat | 窗口正常打开，**无顶部红条**（有红条 = JS 加载/运行错误，先修这个） |
| R0-2 | 导入任意素材 | 素材面板出现缩略图/波形 |
| R0-3 | 拖素材进时间轴 | 时间轴出现片段块，选中正常 |

## 1. 时间轴（编辑核心，5 项）

| # | 操作 | 通过标准 |
|---|------|---------|
| T1 | 导入视频+音频各 1 个 | 两轨都出现片段，位置正确 |
| T2 | 选中片段按 S（或工具栏分割） | 切成两段，中间有分割线，两段各自可选中/拖动 |
| T3 | 拖片段右边缘（trim） | 时长改变，相邻段不重叠；拖回恢复 |
| T4 | 拖动片段到其他位置 | 松手落位正确，吸附生效（N 键切换） |
| T5 | 加第二条轨道（视频/音频/文字） | 新轨出现，片段可跨轨移动 |

**追加检查（修改数据模型后必测）**：分割后拖动右段 → 右段源窗口应保持（预览内容不跳变）；撤销（Ctrl+Z）→ 恢复原状。

## 2. 播放（分两档：可验收 / 冻结）

### 2.1 当前可验收（必须全过）

| # | 操作 | 通过标准 |
|---|------|---------|
| P1 | MP3 单段首播 | 有声，播放头走 |
| P2 | MP4 单段首播 | 画面动 + 有声 |
| P3 | 播放中点暂停 → 再点播放 | 原位置继续，有声 |
| P4 | 拖动播放头到中段 → 点播放 | 从指定位置播 |
| P5 | 播放结束 | 播放头停在末尾，不报错 |

### 2.2 冻结项（已知未修，不作为当前验收标准 ⛔）

| 项 | 现状 | 处置 |
|----|------|------|
| MP3 split 第二段 / 跨段 | 无声（playing 态 seek 设不上，WebView2 元素模式限制） | 冻结，归 C.0 AudioEngine 决策 |
| MP4 split 跨段 | 间歇卡住 | 冻结，同上 |
| 从头重播 | 不稳定 | 冻结，同上 |

> 冻结 ≠ 永远不做。做 C.0 时按「事务 seek barrier + 播放状态隔离」已有纪律重做，届时解开冻结并补回验收。

## 3. 素材/Asset（Step 3 后新增）

| # | 操作 | 通过标准 |
|---|------|---------|
| A1 | 导入新素材 → 拖入时间轴 | 新段带 material_id（控制台 `get_segment_detail` 或后端可查） |
| A2 | 打开旧存档项目 | 旧段无 material_id 也能正常预览（fallback 生效） |
| A3 | 分割同源段 → 拖开右段 | 源窗口连续（snap-once：右段 src_start == 左段 src_end） |

## 4. 记录格式

每次回归在当日日志追加：

```
回归 2026-08-15 21:00（commit cd1547b）
R0: ✅ ✅ ✅
T1-T5: ✅ ✅ ✅ ✅ ✅
P1-P5: ❌ P1 无声（现象：... / 日志：[seek] to=X cur=0）
A1-A3: —
结论：通过 / 有回归（定位中）
```

## 5. 与冻结锚点的关系

- `v0.9-freeze` tag = 功能基线（播放器跨段无声已知未修）
- 架构收口（拆 JS/播放隔离/Asset 分离）**不改变** 2.1 可验收项的通过标准
- 任何「本来过、现在不过」= 回归，必须优先修，不掩盖
